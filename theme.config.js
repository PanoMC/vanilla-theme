/**
 * vanilla-theme is the reference thin theme: it overrides nothing, so every
 * page renders @panomc/theme-core's default views. Use this file to:
 *   - override views:      views: { LoginView: () => import("./src/views/LoginView.svelte") }
 *   - extend the settings schema: settingsSchema: [...]
 */
export default {
  views: {},
};
